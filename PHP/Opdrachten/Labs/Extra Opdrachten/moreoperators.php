<?php
$beschrijving = <<<OPERATORS
een Ternary operators zijn een andere benadering op een If/Else functie.
Als je code te lang wordt kan het niet optimaal zijn om If/Else te gebruiken
Bij een Ternary operator heb je een voorwaarde als aan die voorwaarde wordt
voldaan geeft het de eerste optie terug (de true van het verhaal) en anders
de tweede optie. De false. Dit kan ervoor zorgen dat je kortere structuren krijgt.

Een dot (.) operator zorgt dat strings aan elkaar geplakt worden.

++ en -- doen +1 en -1. Dit is heel simpel als het is.

+= doet het getal + het antwoord voor -= hetzelfde maar met min
net als *= en /= voor maal en delen zijn.
OPERATORS;
