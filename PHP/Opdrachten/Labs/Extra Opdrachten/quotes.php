<?php
$uitleg = <<<TEKST
  What is the difference between '' and ""?
  single quoted strings are plain text with no special case.
  double quoted have the ability to implement variables inbetween.
  "$uitleg" so $uitleg is a variable in this case but not in the:
  '$uitleg' case where its just a string.
TEKST;
